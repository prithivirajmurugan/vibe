import { Inngest } from "inngest";

export const inngest = new Inngest({
  id: "vibe",
  name: "vibe-project",
  // The base URL for your Inngest instance, e.g. "https://api.inngest.com"
  // If you're using a self-hosted Inngest instance, replace this with your instance's URL.
});